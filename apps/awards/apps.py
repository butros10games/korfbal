"""Config for the awards app."""

from django.apps import AppConfig


class AwardsConfig(AppConfig):
    """Config for the awards app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.awards"
