"""Awards / engagement domain.

Currently owns Match MVP voting. This app exists so MVP can grow into a richer
awards/engagement domain (notifications, badges, achievements) without bloating
`schedule`.
"""
